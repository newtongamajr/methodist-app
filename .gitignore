*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
# vendor:publish artifacts we don't customize — Livewire serves its JS
# via the framework route, not from /public/vendor/livewire, and the
# Bootstrap/Tailwind pagination templates are vendor defaults we
# haven't touched. `composer install` re-publishes them when needed.
/public/vendor
/resources/views/vendor
# spatie/pdf-to-image scratch directory + any other transient media
# files written outside the storage/app tree.
/storage/media-library
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
auth.json

# Per-developer Claude Code overrides
.claude/settings.local.json
